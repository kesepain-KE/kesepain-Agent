# Root Python dependencies
#
# Current fact on 2026-04-07:
# - The active main chain uses only Python standard library modules.
# - The currently connected plugins and skills also use only Python standard
#   library modules.
# - This file is intentionally kept without third-party package entries.
#
# Current runtime notes:
# - DeepSeek / OpenAI calls are made through urllib in provider/LLM/openai_api.py
# - web_search uses urllib to access Tavily
# - weather uses PowerShell Invoke-WebRequest and does not require a Python package
#
# Rule:
# - Only add a package here when it becomes required by the shared runtime chain
#   or by a plugin / skill that is expected to work out of the box in this repo.
# - Purely optional or experimental dependencies should stay next to the module
#   that needs them, not be promoted here by default.
