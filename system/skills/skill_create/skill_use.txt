指令1：
    指令内容：/创建技能 <skill目录名> | <指令名称> | <指令说明>
    示例1：
        /创建技能 news_search | 新闻查询 | 查询新闻信息
    注意：
        1.skill目录名仅支持字母、数字、下划线、中划线
        2.指令名称可带或不带 / ，创建时会自动补齐
        3.如果目标目录已存在，会直接返回，不会覆盖已有文件
